import React from "react";
import { Input } from "../ui/input";

type Props = {};

const Search = (props: Props) => {
  return (
    <div>
      <Input type="text" />
    </div>
  );
};

export default Search;
